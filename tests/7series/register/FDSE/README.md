# FDSE — D flip-flop with clock enable and synchronous set

UG953 v2026.1, section FDSE, pages 378–379 (REGISTER / SDR). Work unit: `flops`. GENERATED by `_shared/flops/flop_tests.py`.

## Overview

FDSE is a single D flip-flop with clock enable and a synchronous set input `S` that drives Q High. GSR loads INIT. Programmable inversion exists on C, D and the control pin. Behavioural claims FDSE.C1–C8 are in `catalog/7series/FDSE.overrides.yaml`.

## Tests

| ID | Level | Style | Exercises |
|---|---|---|---|
| `7series.FDSE.L0.smoke` | L0 | vector | port:C, port:CE, port:D, port:Q, port:S, port:C:edge, port:CE:1, port:D:1, attr:INIT=1'b0, attr:INIT=1'b1, attr:IS_C_INVERTED=1'b0, attr:IS_C_INVERTED=1'b1, attr:IS_D_INVERTED=1'b0, attr:IS_D_INVERTED=1'b1, attr:IS_S_INVERTED=1'b0, attr:IS_S_INVERTED=1'b1, claim:FDSE.C1, claim:FDSE.C4 |
| `7series.FDSE.L0.illegal_init` | L0 | vector |  |
| `7series.FDSE.L1.capture` | L1 | vector | port:C, port:CE, port:D, port:Q, port:C:edge, port:CE:1, port:D:0, port:D:1, claim:FDSE.C1 |
| `7series.FDSE.L1.ce_hold` | L1 | vector | port:C, port:CE, port:D, port:Q, port:C:edge, port:CE:0, port:CE:1, port:D:0, port:D:1, claim:FDSE.C2 |
| `7series.FDSE.L1.set_over_ce` | L1 | vector | port:S, port:CE, port:Q, port:CE:0, port:CE:1, port:S:0, port:S:1, claim:FDSE.C3 |
| `7series.FDSE.L1.gsr_init` | L1 | vector | port:Q, claim:FDSE.C4 |
| `7series.FDSE.L1.is_c_inverted` | L1 | vector | port:C, port:Q, port:C:edge, attr:IS_C_INVERTED=1'b1, claim:FDSE.C5 |
| `7series.FDSE.L1.is_s_inverted` | L1 | vector | port:S, port:Q, port:S:0, port:S:1, attr:IS_S_INVERTED=1'b1, claim:FDSE.C6 |
| `7series.FDSE.L1.is_d_inverted` | L1 | vector | port:D, port:Q, port:D:0, port:D:1, attr:IS_D_INVERTED=1'b1, claim:FDSE.C7 |
| `7series.FDSE.L2.exhaustive` | L2 | vector | port:C, port:CE, port:D, port:Q, port:S, port:C:edge, port:CE:0, port:CE:1, port:D:0, port:D:1, port:S:0, port:S:1, attr:INIT=1'b0, attr:INIT=1'b1, attr:IS_C_INVERTED=1'b0, attr:IS_C_INVERTED=1'b1, attr:IS_D_INVERTED=1'b0, attr:IS_D_INVERTED=1'b1, attr:IS_S_INVERTED=1'b0, attr:IS_S_INVERTED=1'b1, claim:FDSE.C1, claim:FDSE.C2, claim:FDSE.C3, claim:FDSE.C5, claim:FDSE.C6, claim:FDSE.C7 |
| `7series.FDSE.L2.random` | L2 | vector | port:C, port:CE, port:D, port:Q, port:S, port:C:edge, port:CE:0, port:CE:1, port:D:0, port:D:1, port:S:0, port:S:1, attr:INIT=1'b0, attr:INIT=1'b1, attr:IS_C_INVERTED=1'b0, attr:IS_C_INVERTED=1'b1, attr:IS_D_INVERTED=1'b0, attr:IS_D_INVERTED=1'b1, attr:IS_S_INVERTED=1'b0, attr:IS_S_INVERTED=1'b1, claim:FDSE.C1, claim:FDSE.C2, claim:FDSE.C3 |
| `7series.FDSE.L1.sv_gsr_midsim` | L1 | sv | port:Q, claim:FDSE.C4 |
| `7series.FDSE.L1.sv_x_inputs` | L1 | sv | port:CE, port:D, port:S, port:Q, claim:FDSE.C2, claim:FDSE.C3 |
| `7series.FDSE.L2.cocotb_random` | L2 | cocotb | port:C, port:CE, port:D, port:Q, port:S, claim:FDSE.C1, claim:FDSE.C2, claim:FDSE.C3, claim:FDSE.C4 |

## Why each test is useful, and what it misses

- `7series.FDSE.L0.smoke`: Every one of the 16 attribute combinations elaborates, powers up to INIT and captures once on every simulator: the minimum any toolchain must get right.
  - Misses: only one capture per configuration; no control, CE-low or GSR activity
  - Misses: no x/z on any input (sv_x_inputs covers x)
  - Misses: illegal values are tried only by L0.illegal_init
  - Not on hw for configurations `*_d1_*`: IS_D_INVERTED=1 (UG953 p379) is only legal on I/O registers; the fabric harness uses SLICE flops
- `7series.FDSE.L0.illegal_init`: INIT=1'bx is outside UG953's 1'b0/1'b1 (p379); the simulation must reject it (expect=reject), which exercises the runtime-rejection path of spec §4.1.
  - Misses: only INIT=1'bx is tried; over-width literals are truncated at elaboration and IS_*_INVERTED illegal values are not tried
  - Misses: whether UNISIM rejects it is observed, not documented (see Task 24)
- `7series.FDSE.L1.capture`: Pins the basic D-to-Q transfer on the active edge, for both INIT values and for the all-defaults configuration (model defaults vs UNISIM defaults).
  - Misses: CE held High and the control inactive throughout
  - Misses: no GSR after power-up
  - Misses: default polarities only
  - Misses: no x/z on any input (sv_x_inputs covers x)
- `7series.FDSE.L1.ce_hold`: Shows that CE Low makes clock edges no-ops even with D different from Q.
  - Misses: CE never toggles within a cycle (between edges only)
  - Misses: control inactive
  - Misses: default polarities only
  - Misses: no x/z on any input (sv_x_inputs covers x)
- `7series.FDSE.L1.set_over_ce`: S must win over CE Low and over D, at the next active edge. A priority inversion here is a classic synthesis-mapping bug.
  - Misses: S is never asserted and released within one clock period
  - Misses: default polarities only
  - Misses: no x/z on any input (sv_x_inputs covers x)
- `7series.FDSE.L1.gsr_init`: A GSR pulse mid-run returns Q to INIT from the opposite value; checks glbl handling in every simulator and, later, every flow's INIT mapping.
  - Misses: GSR overlapping an active control is not driven here
  - Misses: edges during GSR are an inferred behaviour (see sv_gsr_midsim)
- `7series.FDSE.L1.is_c_inverted`: Samples after both edges show capture only on the falling edge.
  - Misses: CE High and control inactive throughout; the other inversions stay 0 (combinations are in L2.exhaustive)
- `7series.FDSE.L1.is_s_inverted`: S held Low acts as active; a flow that drops the inversion fails at once.
  - Misses: one assertion per configuration; other inversions stay 0
- `7series.FDSE.L1.is_d_inverted`: Q takes the complement of D.
  - Misses: claim:FDSE.C8 — IS_D_INVERTED=1 is only legal on I/O registers; a placement rule for hardware flows (steps 3-4), not a simulation behaviour
- `7series.FDSE.L2.exhaustive`: Every attribute combination x prior Q x (control, CE, D): the complete single-edge truth table.
  - Misses: single edge per combination: no multi-cycle ordering (see L2.random)
  - Misses: no GSR (L1.gsr_init and sv_gsr_midsim cover it; hardware GSR needs §7.2)
  - Misses: no x/z on any input (sv_x_inputs covers x)
  - Not on hw for configurations `*_d1_*`: IS_D_INVERTED=1 (UG953 p379) is only legal on I/O registers; the fabric harness uses SLICE flops
- `7series.FDSE.L2.random`: Seeded constrained-random sequences find ordering effects that the single-edge table cannot.
  - Misses: no GSR (hardware GSR needs §7.2; L1.gsr_init covers simulation)
  - Misses: one seed per run; failing seeds must be frozen by hand (xut freeze-seed is deferred)
  - Misses: no x/z on any input (sv_x_inputs covers x)
  - Misses: the control only changes between edges (sync kinds)
  - Not on hw for configurations `*_d1_*`: IS_D_INVERTED=1 (UG953 p379) is only legal on I/O registers; the fabric harness uses SLICE flops
- `7series.FDSE.L1.sv_gsr_midsim`: Direct UNISIM instances for both INIT values; glbl GSR is forced mid-run while clocking. Edges during GSR are recorded, not judged, because UG953 is silent.
  - Misses: behaviour of clock edges while GSR is active: undocumented, checkpoint only
  - Misses: default IS_* polarities only
  - Misses: control inactive throughout
- `7series.FDSE.L1.sv_x_inputs`: X on D, CE or the control: the documented cases (CE Low holds; the control overrides) are checked; the undocumented ones are recorded for cross-simulator comparison. Not run on Verilator: see unsupported_reasons (plan ambiguity 9).
  - Misses: UG953 does not define X behaviour; X on D with CE High, X on CE and X on the control are checkpoints only
  - Misses: no x on C
  - Misses: default polarities only
- `7series.FDSE.L2.cocotb_random`: Long model-checked random sessions on Icarus and Verilator; any failing seed becomes a frozen vector test that also runs on xsim and hardware.
  - Misses: no GSR mid-session
  - Misses: only 4 of the 16 attribute configurations
  - Misses: no x/z on any input (sv_x_inputs covers x)

## Oracle

- Vector tests: the clean-room golden model `models/xut_models/7series/fdse.py` (shared logic in `_common/flops.py`), written from UG953 alone. Every expected bit carries `doc:<page>` or `inferred:<reason>`, and bits UG953 leaves undefined are `-`.
- sv tests: self-checks of documented behaviour only; undocumented X and GSR cases are checkpoints compared between simulators by `xut crosscheck`.
- cocotb: the same golden model, cycle by cycle.
- References: UNISIM on xsim, Icarus and Verilator (after `xut verilatorize`, guarded by its Icarus equivalence check).

## Known gaps (all tests)

- Timing (setup/hold, clock-to-Q, recovery/removal) is out of scope (spec §2).
- only one capture per configuration; no control, CE-low or GSR activity
- no x/z on any input (sv_x_inputs covers x)
- illegal values are tried only by L0.illegal_init
- only INIT=1'bx is tried; over-width literals are truncated at elaboration and IS_*_INVERTED illegal values are not tried
- whether UNISIM rejects it is observed, not documented (see Task 24)
- CE held High and the control inactive throughout
- no GSR after power-up
- default polarities only
- CE never toggles within a cycle (between edges only)
- control inactive
- S is never asserted and released within one clock period
- GSR overlapping an active control is not driven here
- edges during GSR are an inferred behaviour (see sv_gsr_midsim)
- CE High and control inactive throughout; the other inversions stay 0 (combinations are in L2.exhaustive)
- one assertion per configuration; other inversions stay 0
- claim:FDSE.C8 — IS_D_INVERTED=1 is only legal on I/O registers; a placement rule for hardware flows (steps 3-4), not a simulation behaviour
- single edge per combination: no multi-cycle ordering (see L2.random)
- no GSR (L1.gsr_init and sv_gsr_midsim cover it; hardware GSR needs §7.2)
- no GSR (hardware GSR needs §7.2; L1.gsr_init covers simulation)
- one seed per run; failing seeds must be frozen by hand (xut freeze-seed is deferred)
- the control only changes between edges (sync kinds)
- behaviour of clock edges while GSR is active: undocumented, checkpoint only
- default IS_* polarities only
- control inactive throughout
- UG953 does not define X behaviour; X on D with CE High, X on CE and X on the control are checkpoints only
- no x on C
- no GSR mid-session
- only 4 of the 16 attribute configurations

## Runner support and expected divergences

| Test | python | xsim | iverilog | verilator | hw |
|---|---|---|---|---|---|
| `7series.FDSE.L0.smoke` | yes | yes | yes | yes | yes |
| `7series.FDSE.L0.illegal_init` | yes | yes | yes | unsupported: a 2-state simulator cannot represent the 1'bx attribute value | unsupported: rejection of an illegal attribute is a simulation-model check |
| `7series.FDSE.L1.capture` | yes | yes | yes | yes | yes |
| `7series.FDSE.L1.ce_hold` | yes | yes | yes | yes | yes |
| `7series.FDSE.L1.set_over_ce` | yes | yes | yes | yes | yes |
| `7series.FDSE.L1.gsr_init` | yes | yes | yes | yes | unsupported: GSR pulses need the GSR-immune harness state of spec §7.2 |
| `7series.FDSE.L1.is_c_inverted` | yes | yes | yes | yes | yes |
| `7series.FDSE.L1.is_s_inverted` | yes | yes | yes | yes | yes |
| `7series.FDSE.L1.is_d_inverted` | yes | yes | yes | yes | unsupported: UG953 p379: IS_D_INVERTED must be 0 unless the flop is an I/O register; the fabric harness uses SLICE flops |
| `7series.FDSE.L2.exhaustive` | yes | yes | yes | yes | yes |
| `7series.FDSE.L2.random` | yes | yes | yes | yes | yes |
| `7series.FDSE.L1.sv_gsr_midsim` | no: self-checking sv testbench; there is no golden-model replay | yes | yes | yes | unsupported: sv testbenches are simulation-only (spec §4.3) |
| `7series.FDSE.L1.sv_x_inputs` | no: self-checking sv testbench; there is no golden-model replay | yes | yes | unsupported: 2-state simulator: x stimulus is randomised per X seed (spec §5.6), so the undocumented x checkpoints cannot be compared | unsupported: sv testbenches are simulation-only (spec §4.3) |
| `7series.FDSE.L2.cocotb_random` | no: the cocotb test compares against the golden model itself | unsupported: cocotb has no xsim backend (spec §4.3) | yes | yes | unsupported: cocotb runs in simulation; failing seeds are frozen into vector tests |

Findings: none recorded.


## Related tests

- `7series.FDSE.L0.smoke`: `7series.FDRE.L0.smoke`, `7series.FDCE.L0.smoke`, `7series.FDPE.L0.smoke`, `7series.FDSE.L0.illegal_init`
- `7series.FDSE.L0.illegal_init`: `7series.FDRE.L0.illegal_init`, `7series.FDCE.L0.illegal_init`, `7series.FDPE.L0.illegal_init`, `7series.FDSE.L0.smoke`
- `7series.FDSE.L1.capture`: `7series.FDRE.L1.capture`, `7series.FDCE.L1.capture`, `7series.FDPE.L1.capture`, `7series.FDSE.L2.exhaustive`
- `7series.FDSE.L1.ce_hold`: `7series.FDRE.L1.ce_hold`, `7series.FDCE.L1.ce_hold`, `7series.FDPE.L1.ce_hold`
- `7series.FDSE.L1.set_over_ce`: `7series.FDRE.L1.reset_over_ce`, `7series.FDCE.L1.clear_over_ce`, `7series.FDPE.L1.preset_over_ce`
- `7series.FDSE.L1.gsr_init`: `7series.FDRE.L1.gsr_init`, `7series.FDCE.L1.gsr_init`, `7series.FDPE.L1.gsr_init`, `7series.FDSE.L1.sv_gsr_midsim`
- `7series.FDSE.L1.is_c_inverted`: `7series.FDRE.L1.is_c_inverted`, `7series.FDCE.L1.is_c_inverted`, `7series.FDPE.L1.is_c_inverted`
- `7series.FDSE.L1.is_s_inverted`: `7series.FDRE.L1.is_r_inverted`, `7series.FDCE.L1.is_clr_inverted`, `7series.FDPE.L1.is_pre_inverted`
- `7series.FDSE.L1.is_d_inverted`: `7series.FDRE.L1.is_d_inverted`, `7series.FDCE.L1.is_d_inverted`, `7series.FDPE.L1.is_d_inverted`
- `7series.FDSE.L2.exhaustive`: `7series.FDRE.L2.exhaustive`, `7series.FDCE.L2.exhaustive`, `7series.FDPE.L2.exhaustive`, `7series.FDSE.L1.capture`
- `7series.FDSE.L2.random`: `7series.FDRE.L2.random`, `7series.FDCE.L2.random`, `7series.FDPE.L2.random`, `7series.FDSE.L2.cocotb_random`
- `7series.FDSE.L1.sv_gsr_midsim`: `7series.FDRE.L1.sv_gsr_midsim`, `7series.FDCE.L1.sv_gsr_midsim`, `7series.FDPE.L1.sv_gsr_midsim`, `7series.FDSE.L1.gsr_init`
- `7series.FDSE.L1.sv_x_inputs`: `7series.FDRE.L1.sv_x_inputs`, `7series.FDCE.L1.sv_x_inputs`, `7series.FDPE.L1.sv_x_inputs`
- `7series.FDSE.L2.cocotb_random`: `7series.FDRE.L2.cocotb_random`, `7series.FDCE.L2.cocotb_random`, `7series.FDPE.L2.cocotb_random`, `7series.FDSE.L2.random`

## How to run

```bash
uv run xut run '7series.FDSE.*' --jobs 40 > .cache/run-fdse.log 2>&1
uv run xut crosscheck '7series.FDSE.*' > .cache/xc-fdse.log 2>&1
uv run xut status record FDSE
```
